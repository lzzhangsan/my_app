---
description: 全应用证据优先协作——任何问题先证据后改码
alwaysApply: true
---

# 全应用 · 证据优先协作

完整手册：`docs/高效协作调试手册.md`。适用于本应用**一切问题**，不限某一网站或某一现象。用户若附上该文件，严格按文件执行。

## 必须遵守

1. **当前场景实际表现 + 当前仓库代码为准**；别站/别功能经验仅参考，禁止硬套。
2. **先证据后改码**：现象/目标 →（需要时）短前缀分阶段日志 → 清 logcat → 热重启 R 或约定安装 → 用户操作一次 → 筛日志/对照代码 → 只改已证实的那一层因果链。
3. **一次一条因果链**；宁可 fail-closed 说清失败，也不要错误副作用（错媒、假重复、清数据等）。
4. **可复现问题优先本地日志闭环**；不要为已定位问题启动耗时子智能体空搜。
5. **热重启优先**；原生变更才 `adb install -r`。禁止无 `-r` 的 `flutter install`（会卸装清数据）。
6. 交付：证据结论 → 改动原因 → 最小复测。不擅自 commit/push。

## 用户最小输入

`目标或现象 + 操作 + 结果` 即可。成功后再进入下一目标。
