name: change_copy  # 项目标识名，可以保持不变或改为 change
description: "A second copy of the original app."
publish_to: 'none'

version: 1.0.0+3

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.1.0
  cupertino_icons: ^1.0.2
  uuid: ^3.0.4  # 调整为 3.0.4（之前的代码使用此版本）
  shared_preferences: ^2.3.2
  path_provider: ^2.0.0  # 调整为 2.0.0
  permission_handler: ^11.3.0  # 添加权限处理包
  device_info_plus: ^10.1.0
  sqflite: ^2.0.0+4  # 调整为 2.0.0+4
  path: ^1.8.0  # 调整为 1.8.0
  image_picker: ^0.8.0+4  # 调整为 0.8.0+4
  photo_manager: ^3.5.1
  video_player: ^2.8.2
  url_launcher: ^6.2.5
  video_thumbnail: ^0.5.3
  chewie: ^1.7.5  # 更新chewie视频播放UI组件
  flutter_colorpicker: ^1.0.3  # 调整为 1.0.3
  archive: ^4.0.4
  file_picker: ^9.0.2
  share_plus: ^10.1.1
  win32: ^5.8.0
  audioplayers: ^6.4.0
  record: ^5.2.1
  ffmpeg_kit_flutter: ^6.0.3  # 新增：用于视频转码（可选，处理顽固视频）
  better_player: ^0.0.83  # 推荐：作为备用插件